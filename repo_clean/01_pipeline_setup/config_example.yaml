client:
  name: "ClientName"
  base_url: "https://clientwebsite.com"
  industry: "ecommerce"
  language: "de"

scraping:
  max_pages: 50
  probe_pages: 5
  exclude_patterns:
    - "/cart"
    - "/checkout"
    - "/login"
    - "/password"
    - "/wp-admin"
    - "/feed"
  exclude_page_types:
    - "/datenschutz"
    - "/privacy"
  cookie_handling:
    wait_before_ms: 3000
    click_selectors:
      - "a._brlbs-btn-accept-all"
      - "button.accept-all-cookies"
      - "[data-testid='uc-accept-all-button']"
      - ".cc-accept"
    wait_after_ms: 1500

cleaning:
  strip_selectors:
    - "nav"
    - "footer"
    - "header"
    - "#BorlabsCookieBox"
    - ".cookie-banner"
    - ".consent-overlay"
    - "[data-nosnippet]"
  strip_text_patterns:
    - "^Zum Hauptinhalt springen$"
    - "^Cookie-Einstellungen.*$"

processing:
  chunk_size: 800
  chunk_overlap: 100
  embedding_model: "text-embedding-3-small"
  embedding_dimensions: 1536

database:
  table_name: "knowledge_base_v3"
  provider: "supabase"
